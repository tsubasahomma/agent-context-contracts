# Synthetic Allowed Routing And Surface Boundary

This fixture intentionally describes missing-only routing shims and
consumer-owned collaboration surfaces without making a tool or platform the
portable baseline.

Tool-oriented guidance is deferred unless a later local workflow defines an
explicit owner, destination mapping, and validation boundary.

Agent-specific payloads belong in missing-only routing shims and MUST NOT
replace portable contracts as the durable doctrine.
